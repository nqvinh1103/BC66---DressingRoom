function changeBikiniTopImage(imageUrl) {
    document.querySelector('.bikinitop').style.background = `url(${imageUrl})`;
}
function changeBikiniBotImage(imageUrl) {
    document.querySelector('.bikinibottom').style.background = `url(${imageUrl})`;
}
function changeShoesImage(imageUrl) {
    document.querySelector('.feet').style.background = `url(${imageUrl})`;
}
function changeHandbagImage(imageUrl) {
    document.querySelector('.handbag').style.background = `url(${imageUrl})`;
}
function changeNecklaceImage(imageUrl) {
    document.querySelector('.necklace').style.background = `url(${imageUrl})`;
}
function changeHairstyleImage(imageUrl) {
    document.querySelector('.hairstyle').style.background = `url(${imageUrl})`;
}
function changeBackgroundImage(imageUrl) {
    document.querySelector('.background').style.background= `url(${imageUrl})`;
}